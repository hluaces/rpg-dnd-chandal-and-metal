{
  "name": "players-site",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio estático para jugadores (Eleventy)",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --watch",
    "build": "npx @11ty/eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
